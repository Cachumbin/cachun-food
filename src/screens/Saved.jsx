import Logo from "../components/logo";
import NavMenu from "../components/NavMenu";

const Saved = () => {
  return (
    <div>
      <Logo />
      <NavMenu />
      <p>Saved</p>
    </div>
  );
};

export default Saved;
